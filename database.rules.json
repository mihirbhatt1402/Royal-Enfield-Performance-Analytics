{
  "rules": {
    "presence": {
      "$uid": {
        ".read": "auth != null && auth.token.email.contains('@girnarsoft.com')",
        ".write": "auth != null && auth.uid === $uid"
      }
    }
  }
}
